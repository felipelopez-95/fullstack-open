```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST /new_note (note: "Comprar leche")
    activate server
    Note right of server: El servidor guarda la nota
    server-->>browser: Redirect a /notes
    deactivate server

    browser->>server: GET /notes
    activate server
    server-->>browser: HTML con todas las notas
    deactivate server

    Note right of browser: El navegador muestra la página actualizada
```